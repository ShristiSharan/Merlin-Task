import { BsArrow90DegLeft    } from 'react-icons/fa'; // Importing Firefox icon from React Icons

export const LeftArrowIcon = () => {
  return (
    <  BsArrow90DegLeft  size={24} /> // Using the imported Firefox icon with size prop set to 24
  );
};
